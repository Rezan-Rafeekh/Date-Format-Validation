# Date-Format-Validation
A JavaScript program that validates whether a user-entered string contains a date in the dd-mm-yyyy format using Regular Expressions. The program checks for the date pattern anywhere within the string and displays the result using an alert box.
