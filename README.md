# Date Format Validation using JavaScript

##📄 Description

A JavaScript-based program that checks whether a user-entered string contains a valid date in the dd-mm-yyyy format using Regular Expressions. The validation result is displayed through an alert box, making the program simple, interactive, and easy to understand.

---

## ⚙️ Features

* Accepts a string input from the user
* Uses a regular expression to identify the `dd-mm-yyyy` date pattern
* Displays validation result using alert messages
* Handles inputs where the date may appear anywhere within the string

---

## 🛠️ Technologies Used

* HTML5
* JavaScript (ES6)

---

## ▶️ How to Run

1. Open the project folder in **Visual Studio Code**
2. Open `index.html` in a web browser or using **Live Server**
3. Enter a string when prompted to check for date validity

---

## 🎯 Learning Outcomes

* Understanding Regular Expressions in JavaScript
* Performing pattern matching on strings
* Implementing basic input validation

---

## 📂 Project Files

* `index.html`
* `script.js`
